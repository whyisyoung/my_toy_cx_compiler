{
    int a;
    int c;
    bool flag;
    const int maxn = 100;

    function print
    {
        int b;
        b = 3;
        write a;
        write(a + b);
        write(a * b);
        write(a / b);
        write(a % b);
    }

    read a;
    while(a != 0) {
        if(odd(a)) {
            write 1111111;
        };

        if(a > 0) {
            flag = true;
        } else {
            flag = false;
        };

        if(a == 0) {
            write 2222222;
        };

        if(true) {
            write 5555555;
        };

        c = 1;

        call print;

        ++c;
        write c;
        --c;
        write c;
        read a;
    };

}